// @flow

export type IFilter = {

}
/*
partnerId: {
    eq: [partnerId],
  }

  id: {
        eq: [partnerId],
      },

      assignedTagId: {
        eq: [tagId],
      },

      accountId: {
          eq:accountList.getAccountIDs()
        }

        id: {
        eq: [locationId],
      },

      id: {
        eq: [materialId],
      },

      const filter = {
        isActivated: {
          eq:true
        },
      }

      tagAllocationType: {
            os:["BATCH","MATERIAL"]
        }

        id: {
        eq: [tagId],
      },

      bulkUpload: {
        eq: true,
      },

      userRole: {
          os: ["USER","ADMIN","MODERATOR"]
        },
        isRemoved:{
          eq: false
        }
*/
// export type IdsFilter = {
//     eq: Array<number>,
//   }
  
//   export type EqualityFlags = {
//     startWith?: ?boolean,
//     endWith?: ?boolean,
//     caseSensitive?: ?boolean,
//   }
  
//   export type StringFilter = {
//     eq: string,
//     flags?: ?EqualityFlags,
//   }
  
//   export type BooleanFilter = {
//     eq: boolean,
//   }
  
//   export type OrderBy = 'ASC' | 'DESC'
  
//   export type InstantFilter = {
//     eq?: ?Date,
//     gt?: ?Date,
//     gte?: ?Date,
//     lt?: ?Date,
//     lte?: ?Date,
//   }
  