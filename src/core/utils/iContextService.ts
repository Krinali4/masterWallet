export interface iContextService extends iContextSideBarService {
    accessTokenValid?: boolean,
    setAccessTokenValid?: React.Dispatch<React.SetStateAction<boolean>>,
}

export interface iContextSideBarService {
    
}